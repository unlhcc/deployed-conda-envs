# This is an Anaconda project file.
#
# Here you can describe your project and how to run it.
# Use `anaconda-project run` to run the project.
# The file is in YAML format, please see http://www.yaml.org/start.html for more.
#

#
# Set the 'name' key to name your project
#
name: entrez-direct

#
# Set a one-sentence-or-so 'description' key with project details
#
description: "Entrez Direct (EDirect) is an advanced method for accessing the NCBI's\
  \ set of interconnected databases (publication, sequence, structure, gene, variation,\
  \ expression, etc.) from a UNIX terminal window. "
#
# In the platforms section, list platforms the project should work on
# Examples: "linux-64", "osx-64", "win-64"
# Use `anaconda-project add-platforms` to add platforms.
#
platforms:
- linux-64
env_specs:
  entrez-direct-10.0.20180927:
    packages:
    - entrez-direct=10.0
    channels: []
  entrez-direct-11.0:
    packages:
    - entrez-direct=11.0
    - perl=5.26
    channels: []
  entrez-direct-13.3:
    packages:
    - entrez-direct=13.3
    channels: []
  entrez-direct-16.2:
    packages:
    - entrez-direct=16.2
    - perl
    channels: []
