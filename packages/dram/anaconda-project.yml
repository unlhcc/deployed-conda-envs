# This is an Anaconda project file.
#
# Here you can describe your project and how to run it.
# Use `anaconda-project run` to run the project.
# The file is in YAML format, please see http://www.yaml.org/start.html for more.
#

#
# Set the 'name' key to name your project
#
name: DRAM

#
# Set a one-sentence-or-so 'description' key with project details
#
description: DRAM (Distilled and Refined Annotation of Metabolism) is a tool for annotating
  metagenomic assembled genomes and VirSorter identified viral contigs.


#
# In the platforms section, list platforms the project should work on
# Examples: "linux-64", "osx-64", "win-64"
# Use `anaconda-project add-platforms` to add platforms.
#
platforms:
- linux-64
env_specs:
  dram-1.0.6:
    packages:
    - dram=1.0.6
    - python=3.7
    channels: []
  dram-1.1.1:
    packages:
    - dram=1.1.1
    - python=3.7
    channels: []
#commands:
#  download-db:
#    unix: mkdir -p $HCC_APPS_DB_BCRF_PATH/dram/1.1.1 && DRAM-setup.py prepare_databases --output_dir $HCC_APPS_DB_BCRF_PATH/dram/1.1.1
#    env_spec: dram-1.1.1
